# Development-only environment variables
# This file is used only when running 'npm start' locally

# Disable host check for local development to fix webpack dev server error
DANGEROUSLY_DISABLE_HOST_CHECK=true

# Google OAuth Client ID
# WARNING: Create React App only exposes env vars with REACT_APP_ prefix
# To use REACT_APP_GOOGLE_CLIENT_ID, you need custom webpack config
REACT_APP_GOOGLE_CLIENT_ID=your_REACT_APP_GOOGLE_CLIENT_ID_here

# API URL for local development (backend should be running on port 4000)

